version: 2

exposures:

  - name: weekly_metrics
    label: srinivas by the Week
    type: dashboard
    maturity: high
    description: 
     For more exposure about project details

    depends_on:
      - ref('final_business_table')
      - ref('order_merge')
      - ref('order_macro_data')
      - ref('business_table')
      - source('datafeed_shared_schema','stg_orders_data')
      - source('datafeed_shared_schema','stg_customer_data')
      - source('datafeed_shared_schema','stg_payments')
  
    owner:
      name:  srinivas
      email: srinivasaetl99@gmail.com
